define(function(require) {

    /*
    * Sorting comparators
    */

    return {
        // sort numerically
        // var sortedCounts = counts.sort(numeric);
        numeric: require('./sort/numeric')
    };

});
